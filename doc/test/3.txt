QString tmp = oriStr;
            tmp.replace(subStr,str);
            int pos = tmp.indexOf(str);
            for (QChar *it=tmp.begin(); it!=tmp.end(); ) {
                tmp.remove(pos+str.p1()-1, 1);
                pos = tmp.indexOf(XXX, pos+str.p1());
                if (-1 == pos) {
                    it = tmp.end();
                }
            }
QString tmp = oriStr;
            tmp.replace(subStr,str);
            int pos = tmp.indexOf(str);
            for (QChar *it=tmp.begin(); it!=tmp.end(); ) {
                tmp.remove(pos+str.p1()-1, 1);
                pos = tmp.indexOf(111, pos+str.p1());
                if (-1 == pos) {
                    it = tmp.end();
                }
            }
QString tmp = oriStr;
            tmp.replace(subStr,str);
            int pos = tmp.indexOf(str);
            for (QChar *it=tmp.begin(); it!=tmp.end(); ) {
                tmp.remove(pos+str.p2()-1, 1);
                pos = tmp.indexOf(222, pos+str.p2());
                if (-1 == pos) {
                    it = tmp.end();
                }
            }
QString tmp = oriStr;
            tmp.replace(subStr,str);
            int pos = tmp.indexOf(str);
            for (QChar *it=tmp.begin(); it!=tmp.end(); ) {
                tmp.remove(pos+str.p3()-1, 1);
                pos = tmp.indexOf(333, pos+str.p3());
                if (-1 == pos) {
                    it = tmp.end();
                }
            }
QString tmp = oriStr;
            tmp.replace(subStr,str);
            int pos = tmp.indexOf(str);
            for (QChar *it=tmp.begin(); it!=tmp.end(); ) {
                tmp.remove(pos+str.p4()-1, 1);
                pos = tmp.indexOf(444, pos+str.p4());
                if (-1 == pos) {
                    it = tmp.end();
                }
            }
